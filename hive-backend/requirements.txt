# Hive Bridge API — Python dependencies
# Install with:  pip install -r requirements.txt
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
httpx>=0.27.0          # for async HTTP calls to Hive internals (if needed)
python-dotenv>=1.0.0   # .env support
